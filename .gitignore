node_modules/
.wrangler/
.dev.vars
*.log

# Vite build output for /app and /d's landing page -- regenerated by
# `bun run build:client` / `build:landing` (both run automatically as part
# of `bun run deploy`).
public/app/
public/landing/
